#include<stdio.h>
int main()
{
	int arr[100],n,i,sum=0;
	printf("enter no of elements in the array");
	scanf("%d",&n);
	printf("\n enter elements");
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	
	for(i=0;i<n;i++)
	{
		sum=sum+arr[i];
	}
	printf("the sum is equal to %d",sum);
	return 0;
}
